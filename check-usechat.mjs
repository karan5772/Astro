import { renderHook } from '@testing-library/react-hooks';
// Oh wait, testing-library is not installed. Let's just run a script that imports it?
// We can't easily execute react hooks outside a component. But we can just use the manual fetch!
